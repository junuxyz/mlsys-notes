# Baseline Engine

<p align="center">
  <img src="../../assets/labs/baseline/baseline_engine_demo.gif" alt="baseline engine demo" width="720" />
</p>
<p align="center"><em>baseline engine running with <code>max_batch_len = 2</code></em></p>


`baseline_engine.py` is an extended version of [`microengine`](../microengine/README.md) that keeps most of the same code while adding observability, sampling configuration, token streaming, and performance measurement.

## Usage

```bash
python -m labs.baseline.example
```
